/**
 * Empty module used by Metro to shim Node.js built-ins that don't exist
 * in React Native. The @enbox/* SDK packages dynamically import these
 * with guards, so they just need to resolve without crashing the bundler.
 */
export {};
